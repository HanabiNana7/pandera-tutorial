# Pandera Tutorial

使用 [Pandera](https://github.com/unionai-oss/pandera) 套件，針對 `pandas` 的 `DataFrame` 或 `Series` 進行資料驗證。

---

## 功能說明

- 使用兩種 Pandera 驗證方式：
  - 顯式 Schema：`DataFrameSchema`
  - 類別導向 Schema：`DataFrameModel`
- 完整資料驗證流程展示
- 可執行 Unit tests
- 支援 Jupyter Notebook 操作

